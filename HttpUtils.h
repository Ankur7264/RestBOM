#pragma once
#include <string>

// Callback function required by cURL to capture HTTP responses
inline size_t WriteCallback(void* contents, size_t size, size_t nmemb, void* userp) {
	((std::string*)userp)->append((char*)contents, size * nmemb);
	return size * nmemb;
}
